import { AppShell } from "@/components/AppShell"

export default function RewardsPage() {
  return (
    <AppShell>
      <main className="container section">
        <span className="kicker">Rewards</span>
        <h2>Rewards will be defined soon</h2>
        <p>
          This space will later convert consistent daily practice, balanced wellbeing, gratitude, learning and recovery into meaningful recognition.
        </p>
        <section className="form-card" style={{ marginTop: 16 }}>
          <h3>Coming next</h3>
          <p>Reward rules, badges, streaks and community contribution points will be added after the scoring model is finalized.</p>
        </section>
      </main>
    </AppShell>
  )
}
